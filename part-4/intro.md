# Part IV — Object-Oriented Go

Explore how Go supports object-oriented design without traditional inheritance. We learn about struct methods, receiver semantics (value vs pointer), interface types, composition, embedding, and standard interface paradigms. Finally, we implement an OOP-based storefront web server.
