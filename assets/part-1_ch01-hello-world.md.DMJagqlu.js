import{_ as o,C as n,o as i,c as s,a2 as t,E as r}from"./chunks/framework.BWHo9ZVz.js";const k=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"part-1/ch01-hello-world.md","filePath":"part-1/ch01-hello-world.md"}'),l={name:"part-1/ch01-hello-world.md"};function h(d,e,p,c,g,u){const a=n("VideoPlayer");return i(),s("div",null,[e[0]||(e[0]=t('<h2 id="chapter-1-hello-world" tabindex="-1">Chapter 1: Hello World <a class="header-anchor" href="#chapter-1-hello-world" aria-label="Permalink to &quot;Chapter 1: Hello World&quot;">​</a></h2><div class="slide-visualization-box no-print"><details><summary>📊 Slide Reference: <code>/slides/go-01-hello-slides.pdf</code></summary><div class="slide-iframe-container"><iframe src="/go-lang-matt/slides/go-01-hello-slides.pdf" width="100%" height="450px" style="border:none;"></iframe></div></details></div>',2)),r(a,{videoId:"A9HfEhvpOEY",chapter:"01"}),e[1]||(e[1]=t(`<p>I want to start by talking about the simplest program.</p><h3 id="the-go-playground" tabindex="-1">The Go Playground <a class="header-anchor" href="#the-go-playground" aria-label="Permalink to &quot;The Go Playground&quot;">​</a></h3><p>I&#39;m going to start by using the Go Playground. I&#39;m not going to install Go yet — I&#39;m just going to use this very simple environment that the Go team provided us. I&#39;m going to go to <code>play.golang.org</code> and make a small change. We&#39;ll call this &quot;Hello World&quot; — that&#39;s the prototypical original program. I&#39;m just going to click the Run button, and when I do that, the program actually executes and provides some output right in the web browser.</p><p>The Playground is great for running simple programs, but it has a couple of limitations. It&#39;s really all about I/O — you can&#39;t read or write a file, you can&#39;t open a network socket, run a web server, anything like that. Of course, part of that&#39;s for security.</p><p>The Playground is actually useful in another context. If we go to the Go documentation page and look at the packages — the standard library — we&#39;ll find the <code>fmt</code> package that does formatted I/O. If we look at examples, we can click on an example and we get a description of the <code>Println</code> function, but it has an example that builds in the Playground. I can actually run that example just by clicking on the Run button. And it&#39;s editable — I can actually change this program.</p><h3 id="understanding-the-simple-program" tabindex="-1">Understanding the Simple Program <a class="header-anchor" href="#understanding-the-simple-program" aria-label="Permalink to &quot;Understanding the Simple Program&quot;">​</a></h3><p>Now I want to talk about this simple program that we just wrote, because I want to explain a couple things.</p><p>First of all, every program has to have a <code>main</code> function, and that tells Go where the program starts. It always starts in the <code>main</code> function.</p><div class="language-go vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">go</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">package</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> main</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">fmt</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">func</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> main</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    fmt.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Println</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Hello, World!&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Go is a modular language — we can put the program in different files and compile it together, which is different from when I was a kid when the program had to be in one big file (or in my case, one big stack of punch cards). In Go, we can put different parts of the program into packages, but the <code>main</code> function has to be in a <code>package main</code>. So we need this declaration at the top that says, &quot;Hey, this file is going to be part of package main.&quot;</p><p>The other thing we need to do is import any package we use. We looked at the <code>fmt</code> package and <code>Println</code> — in order to use it, we need an import. We import the <code>fmt</code> package, and then when we call it, we call <code>fmt.Println</code> — we put <code>fmt</code> in front of it, so we&#39;re calling the package dot the function name.</p><p>There&#39;s a connection between importing <code>fmt</code> and using <code>fmt</code>. There&#39;s a connection between declaring a <code>main</code> function and putting it in <code>package main</code>. Those all go together. And the only other thing to point out is the <code>func</code> keyword that we put in front of a function declaration.</p><h3 id="other-environments" tabindex="-1">Other Environments <a class="header-anchor" href="#other-environments" aria-label="Permalink to &quot;Other Environments&quot;">​</a></h3><p>There&#39;s another online environment that&#39;s very convenient called Replit, and it has one advantage: you can actually write a program that does I/O. You can read or write files, open a socket, or read from the console. So it&#39;s a little more sophisticated than the Playground environment.</p><h3 id="installing-go" tabindex="-1">Installing Go <a class="header-anchor" href="#installing-go" aria-label="Permalink to &quot;Installing Go&quot;">​</a></h3><p>I want to briefly mention how to install Go. I&#39;m not going to go into details — you go to the Go downloads page and there&#39;s instructions there. I typically use Homebrew on a Mac, so I just do <code>brew install go</code>. For Linux, you can download a gzip tar file and install that with <code>sudo</code>, and it puts things in the right place, but then you still have to remember to modify your <code>PATH</code>.</p><p>Once we&#39;ve installed Go on our laptop, we can run from the command line. I can create an arbitrary directory, copy my little <code>main.go</code> program with Hello World in it, and then just do:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">go</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .</span></span></code></pre></div><p>Which says &quot;run what&#39;s in this directory.&quot; This is enough for a simple program that uses the standard library. If we want to use third-party software, we&#39;re going to have to do a bit more work, and I&#39;m not ready to talk about that yet.</p><p>The other thing about <code>go run</code> is that it both compiles and runs your program — it compiles it, sticks it in some temp directory, runs it, and then gets rid of what&#39;s left over. Later on I&#39;ll talk about how to actually build a program that we want to deploy as a binary, either by itself or put it into a container.</p><hr>`,21))])}const y=o(l,[["render",h]]);export{k as __pageData,y as default};
